# Lokalny development: wskaż na lokalny backend
# VITE_API_BASE=http://localhost:8000/api

# Google Cloud Run (frontend i backend na tym samym URL — nie ustawiaj tej zmiennej,
# domyślny fallback "/api" działa automatycznie dla tej konfiguracji)
# VITE_API_BASE=https://twoj-service.run.app/api
