"""
sources — fundament multi-source scraperów (wyniki/HT/kursy).

Wspólny interfejs (MatchData, ResultsSource) + agregator porównujący dane
z wielu źródeł (rozjazdy = sygnał) i konsensus (fallback settlement gdy
podstawowe źródło nie pokrywa meczu).
"""
