# Audyt FootStats — 2026-07-07

**Zakres:** audyt inline (limit subagentów do 07-08 19:00 — pełny workflow 10-wymiarowy do wznowienia: run `wf_fcc57641-331`). Pokryte: security sweep repo, świeży kod „analizy" (4 commity 07-06/07), spójność flag env, dryf dokumentacji, weryfikacja carry-over z audytu 2026-06-27.
**NIE pokryte głęboko (do workflow po resecie):** matematyka core (Poisson/DC/Kelly), scrapers, jakość testów, infra deep-dive.

---

## 🔴 HIGH

### H1 — `POST /api/analyses/llm` publiczny + prompt kontrolowany przez klienta
`src/footstats/api/routes/analyses.py:84-99`. Endpoint bez auth (router montowany bez `Depends`, brak `require_user`); frontend woła go czystym `fetch` bez tokenu (`MatchAnalysisView.jsx:79`). Klient wysyła **cały** `card: dict`, z którego budowany jest prompt (`analysis_prompt(card)`) → każdy z internetu może: (a) palić tokeny Groq (limiter globalny 60/min/IP to jedyna zapora), (b) wstrzykiwać dowolną treść do prompta, (c) zaśmiecać `analysis_cache`.
**Fix:** auth dependency (spójnie z coupons) + karta budowana **server-side** po identyfikatorze meczu — nie ufać body. → BP-01/T1,T2.

### H2 — Zakładka „Analizy" nie działa na prod (Cloud Run)
`Dockerfile.api:22-24` kopiuje tylko `pyproject.toml`, `src/`, `gui/dist` — **NIE kopiuje `data/`**. `get_team_stats`/`team_goal_shares_recent` czytają SQLite `DB_PATH` → na prod plik nie istnieje (sqlite3 utworzy pusty) → karty bez goli/mecz, ratingu, strzelców, goal_share kontuzji. Dodatkowo `analysis_cache` (SQLite, `match_analysis.py:151-180`) na efemerycznym FS kontenera → cache znika przy każdym restarcie/instancji.
**Fix:** GCS-pull SQLite przy starcie (synergia z planem parquet P2 z TODO) albo cache/stats do Neon. → BP-01/T7.

### H3 (carry-over 06-27) — `af_budget` RMW bez locka
`src/footstats/utils/logging.py:330` (`bezpieczny_budget_use`) — read-modify-write `af_budget.json` bez locka; równoległe procesy (API + job) mogą zgubić inkrement → przekroczenie quoty API-Football. Czeka na decyzję: filelock (cross-proc) vs threading.Lock (in-proc). → BP-03/D1.

### H4 (carry-over 06-27) — `daily_io` DRAFT nie idempotentny
Podwójne `process_bet` przy powtórce fazy = podwójny zapis (MONEY path). Wymaga nadzoru usera. → BP-03/D2.

### H5 (carry-over 06-27) — `_wyciagnij_json` cicho fabrykuje wynik
`src/footstats/ai/analyzer_helpers.py:387` — nieparsowalna odpowiedź LLM → sentinel `{"typ": "brak", "pewnosc": 0, ...}` bez błędu; pipeline traktuje jak prawdziwą odpowiedź. Zmiana = decyzja (silent→error zmienia zachowanie). → BP-03/D3.

---

## 🟠 MEDIUM

### M1 — `POST /analyses/llm`: brak walidacji wejścia → 500
`analyses.py:85-87` — `card: dict = Body(...)`; `card_data_hash` robi `card["home"]` → body bez klucza = `KeyError` = nieobsłużone 500. Walidacja na granicy systemu (Pydantic) — reguła projektu. → BP-01/T1.

### M2 — `_norm(1.0)` → 1.0% zamiast 100%
`analyses.py:33-37` — heurystyka `p if p > 1 else p * 100`: prawdopodobieństwo dokładnie 1.0 (pewniak, np. Spain 0 straconych) czytane jako 1%. Ustalić skalę `pred_ml` u źródła i usunąć heurystykę. → BP-01/T3.

### M3 — `get_team_stats` bez walk-back sezonu + `_SEASON=2026` hardcode
`player_db.py:246` — exact-match sezonu (w przeciwieństwie do `team_goal_shares_recent:176`, który ma lookback=2). `analyses.py:30` hardcoduje 2026 → kluby (dane primary = sezon 2025) dostają `None` w statystykach nawet lokalnie; prompt LLM zawiera literalne „None strzelone". → BP-01/T4.

### M4 — Silent swallow w cache analiz
`match_analysis.py:162,179-180` — `except sqlite3.Error: return None` / `pass` bez logu. Reguła projektu: nigdy nie połykaj cicho. → BP-01/T5.

### M5 (carry-over 06-27) — `system_paper` idempotencja nieatomowa; double auto-trainer spawn bez guarda
Otwarte z czerwca, wymagają decyzji. → BP-03/D4,D5.

---

## 🟡 LOW

- **L1** — `analysis_cache` bez eviction/TTL; sprawdzić czy `scripts/evict_cache.py` zna nową tabelę (prawdopodobnie nie). → BP-01/T5.
- **L2** — Dryf liczb w docs: STATUS.md „1448 passed" (07-03) / TODO.md „~1436" / PROJECT_STRUCTURE.md „~1440" / **README badge „tests-1346"** (najbardziej stale); TODO „coverage 57" vs CI gate `--cov-fail-under=56` (ci.yml:103). Jedno źródło prawdy + scribe. 
- **L3** — GUI `MatchAnalysisView.jsx`: emoji `⚽` (:49) zamiast lucide 16px (design system: tylko lucide 16/20); `key={i}` po indeksie (:60,:170); `fetch` bez `r.ok` check (:83); `useEffect` bez AbortController (:149). → BP-01/T6.
- **L4** — `player_db.py:73` — f-string w `ALTER TABLE {col} {typ}`; bezpieczne (stały `_OPTIONAL_COLS`), ale wzorzec bez walidacji — dopisać assert/komentarz.
- **L5** — `analysis_prompt` wstawia `None` do tekstu gdy brak stats (`match_analysis.py:123-125`) — LLM dostaje „None strzelone" zamiast „brak danych".
- **L6** (08.07) — CVE PYSEC-2026-1325 w `ecdsa 0.19.2` (przechodnia z `python-jose[cryptography]`): auth = HS256, ECDSA nieużywane w runtime, brak fix version upstream → ignore w CI z komentarzem. Docelowo: migracja `python-jose` → `PyJWT` (aktywniej utrzymywany, bez ecdsa) — task po powrocie.

---

## ✅ Zweryfikowane czyste
- Sekrety: `.env` NIE trackowany w git; brak kluczy (gsk_/sk-/AIza/postgres://) w repo; `.env.example` tylko placeholdery.
- SQL: parametryzacja wszędzie (jedyny f-string = L4, bezpieczny).
- Brak `shell=True` / `eval` / `pickle.load` w src.
- Flagi env (`GROQ_TIP_OVERRIDE` default ON, `SELECTION_MIN_CONF`, `LEAGUE_GATING`, `ENSEMBLE_MARKET_WEIGHT`, `CALIBRATION_ENABLED`, `QUICK_PICKS_USE_POISSON_CACHE`) — odczyt spójny, defaulty zgodne z TODO.
- `migrations.py` — `psycopg2.Error` w `_DB_ERRORS` (HIGH z czerwca NAPRAWIONY, :15).
- `bzzoiro.py` — `requests.RequestException` (subclass OSError) → pokryty przez except endpointu.
- Rate-limiter globalny 60/min/IP (slowapi) + twardsze limity na auth.

## Następny krok
Po resecie limitu (07-08 19:00): wznowić pełny workflow — `Workflow({scriptPath: <session>/workflows/scripts/footstats-full-audit-*.js, resumeFromRunId: "wf_fcc57641-331"})` — core/scrapers/testy/infra deep-dive. Naprawy: `docs/blueprints/BP-01`.
