name: Daily DB Backup

on:
  schedule:
    - cron: '0 5 * * *'   # 05:00 UTC daily (07:00 Warsaw)
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Authenticate to GCP
        uses: google-github-actions/auth@c200f3691d83b41bf9bbd8638997a462592937ed # v2.1.13
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@e427ad8a34f8676edf47cf7d7925499adf3eb74f # v2.2.1

      - name: Download current DB from GCS
        run: |
          gcloud storage cp \
            gs://${{ secrets.BUCKET_NAME }}/data/footstats_backtest.db \
            data/footstats_backtest.db || echo "No existing DB in GCS, skipping"

      - name: Run backup
        env:
          BUCKET_NAME: ${{ secrets.BUCKET_NAME }}
        run: bash scripts/backup_db.sh

      - name: Prune backups older than 30 days
        run: |
          CUTOFF=$(date -u -d '30 days ago' +%Y%m%d)
          gcloud storage ls gs://${{ secrets.BUCKET_NAME }}/backups/ \
            | grep "footstats_backtest_[0-9]" \
            | while read f; do
                FDATE=$(echo "$f" | grep -oP '\d{8}')
                if [[ "$FDATE" < "$CUTOFF" ]]; then
                  gcloud storage rm "$f"
                fi
              done
